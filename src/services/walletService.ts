// Serviço de integração com a extensão MultiversX
export const connectWallet = async () => {
  // Lógica de conexão real com a extensão
};

export const disconnectWallet = async () => {
  // Lógica de desconexão
};

export const createWallet = async () => {
  // Lógica de criação de carteira
};

export const importWallet = async (seed: string) => {
  // Lógica de importação de carteira
};

export const getBalance = async (address: string) => {
  // Lógica para buscar saldo
}; 